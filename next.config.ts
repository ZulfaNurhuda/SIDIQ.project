/**
 * @project SIDIQ.project
 * @author ZulfaNurhuda
 * @github https://github.com/ZulfaNurhuda/SIDIQ.project
 * @description File ini adalah file konfigurasi utama untuk aplikasi Next.js.
 * Ini memungkinkan penyesuaian perilaku Next.js, seperti perutean, proses build, dan variabel lingkungan.
 */

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /**
     * @description Tambahkan opsi konfigurasi Next.js di sini.
     * @example
     * ```
     * reactStrictMode: true,
     * swcMinify: true,
     * images: { domains: ['example.com'] },
     * experimental: { appDir: true },
     * ```
     */
};

export default nextConfig;